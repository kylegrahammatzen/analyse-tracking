#
#      Analyse
# Version - 1.0.0
#
# Use https://docs.analyse.net for plugin documentation.
#
# Configure the following to store player data locally before sending it to Analyse.
# Make sure that the following is the same as the database configuration on your other servers.
database:
  host: 'localhost'
    port: '3306'
    username: 'root'
    password: 'password'